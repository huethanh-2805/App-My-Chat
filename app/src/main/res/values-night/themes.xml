<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyChat" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <!-- Dark mode -->
    <!-- Background của màn hình nếu dark mode bật -->
    <style name="SCREEN">
        <item name="android:background">#232323</item>
    </style>

    <!-- Background của text nếu dark mode bật -->
    <style name="TEXT">
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <!-- Background của button nếu dark mode bật -->
    <style name="BUTTON">
        <item name="android:background">@drawable/btn_start</item>
        <item name="android:textColor">#FFF</item>
    </style>
</resources>