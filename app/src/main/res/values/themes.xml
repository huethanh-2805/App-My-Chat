<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyChat" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyChat" parent="Base.Theme.MyChat" />

    <!-- Light mode -->
    <!-- Background của text nếu light mode bật -->
    <style name="TEXT">
        <item name="android:textColor">#000</item>
    </style>

    <!-- Background của edit text nếu light mode bật -->
    <style name="EDITTEXT">
        <item name="android:background">@drawable/border</item>
    </style>

    <!-- Background của button nếu light mode bật -->
    <style name="BUTTON">
        <item name="android:background">@drawable/btn_start</item>
        <item name="android:textColor">#FFF</item>
    </style>
</resources>